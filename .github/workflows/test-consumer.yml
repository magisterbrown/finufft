name: Test Consumer

on:
  workflow_dispatch:


jobs:
  consume:
    runs-on: ubuntu-latest
    steps:
      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: test-artifact
          path: ./artifacts

      - name: Print artifact content
        run: |
          echo "Artifact content retrieved:"
          ls
